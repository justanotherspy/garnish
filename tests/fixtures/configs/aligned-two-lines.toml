# fixture: subscription-full
# columns: 80
# now: 1738425600
# icons: unicode
# Two lines whose first, last and rightmost modules all differ in width, so
# `align` must pad column 1 on the right, leave the last module alone and pad
# the right group on the left for the bars to stack (SPEC § 4).

align = true

[[line]]
modules = ["model", "session"]
right   = ["session", "api"]

[[line]]
modules = ["session", "clock"]
right   = ["model", "lines"]
