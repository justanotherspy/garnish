# fixture: subscription-full
# columns: 60
# now: 1738425601, 1738425603
# icons: unicode
# env: GARNISH_ANIMATE=0
# The same ticker with animations off: the line is cut with `…` at both
# instants, like `overflow = "truncate"`, instead of a window frozen at
# offset 0 with no mark (SPEC § 4.1, decided 2026-09-06).

overflow   = "ticker"
ticker_gap = " · "

[[line]]
modules = ["model", "effort", "context", "session", "api", "cache"]
right   = ["clock"]
