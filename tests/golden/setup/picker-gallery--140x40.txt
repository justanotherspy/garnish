preview  subscription-full: a Pro/Max session with every field set  ·  140 cols, box 136  ·  2 lines
  ╭─  ~/projects/garnish       │  ━━━━━━━━━━━━━━━━───────────────────────┃ 42% ⤓99% 1.0M ───────────────────────────────── ⠋ 16:00:00 ─╮
  ╰─  ██▊░░░░░░░░░ 24%  2h13m │  ████▉░░░░░░░ 41% ────────────────────────────────────────────────────────────────  +156 −23 (+133) ─╯
bars-and-limits  40-cell line-style context bar with window tag, mini bars on the limits  needs nerd-font  designed for 130 columns
 default                  four lines, every module at its default
 minimal                  one unframed line, the bare values
 full                     four lines, everything each module knows
 compact                  two lines
 animated-dots            dots travelling along the rule, a pulsing separator and a cycling model icon
 ascii-only               7-bit ASCII throughout: ascii icons, a custom +-| frame, no colour codes at all
 bars-and-limits          40-cell line-style context bar with window tag, mini bars on the limits
 boxed-panels             two titled boxes, one around the repo rows and one around usage
 compact-aligned          two rounded lines with stacked bars, Catppuccin Mocha
 compaction-watch         the context bar measured to the compaction point, resets as clock times, line-style bars
 dashboard-panels         a full-height repo box, a centred column, and three stacked panels
 dracula-256              Dracula with role and per-module colour overrides in 256-colour mode
 emoji-overrides          emoji icons with per-module glyph overrides and name limits
 full-aligned             every module at full verbosity, columns aligned, fixed timers
 grid-six                 six equal columns, one module each, over a full-width flex row
 grid-three               three columns side by side — repo, model, usage — reading left, centre, right
 labels-and-placeholders  labels, brackets, dim – for absent modules, UTC clock with date
 links-and-shortcuts      clickable branch and pull request, a fish-style path, two link buttons in fixed boxes
 minimal-clean            one unframed line: path, context, limit, clock
 motd-ticker              repo line plus a scrolling message of the day in a fixed 24-cell box
 narrow-unicode           three short unframed rows for a 72-column pane, no Nerd Font needed, capped modules
 packed-heavy             custom heavy frame, left-packed rows, a separator per row
 session-detail           session, api, cache and cost detail, plain stale style, 1 s git refresh
 sidebar-panels           a 34-cell boxed sidebar, a two-share stack of titled rows, and a bottom-aligned column
 single-line-full         everything on one row, always scrolling as a ticker (200 columns is a comfortable window)
 slow-motion              half-speed motion: a rule pattern drifting left, a pulsing bar, an eight-dot spinner, a scrolling note
 still-life               nothing moves: animation off, no spinner or seconds, fixed-width timers, plain stale values
 tall-eight-lines         one module per row, eight rows, square frame
 three-lines-double       repo / model / timers in a double frame
 ticker-two-step          a long first row scrolling two cells a tick behind a still clock, a second row that fits, 90 columns
 titled-sections          a title in every rule, left, centred and right, a titled spacer and one boxed row, Nord
 two-lines-powerline      location and model only, powerline caps, no colour



↑↓ preset  enter use it  e edit it  f payload  w width  esc back  ? help
